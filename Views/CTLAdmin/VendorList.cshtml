@{
    ViewData["Title"] = "Vendor configuration";
}

@section PageStyles {
    <link href="~/css/datatables.min.css" rel="stylesheet" />
    <link href="~/css/datatables.css" rel="stylesheet" />
    <link href="~/css/vendorlist.css" rel="stylesheet" />
    <link href="~/css/bt-theme.css" rel="stylesheet" />
}

<div class="card bt-card mb-4">
    <div class="bt-page-header">
        <h5><i class="ri-store-2-line"></i> @ViewData["Title"]</h5>
        <input type="hidden" id="adminRole" value="@ViewData["UROLEADMIN"]" />
    </div>

    <div class="bt-form-panel" id="spanAdmin" style="display:none">
        <div class="bt-form-panel-label">Add / edit vendor</div>
        <div class="row">
            <div class="col-md-3">
                <input class="form-control" type="hidden" id="VenCodeTmp" />
                <input class="form-control" type="text" id="VenCode" placeholder="Vendor Code" />
            </div>
            <div class="col-md-4">
                <input class="form-control" type="text" id="VenName" placeholder="Vendor Name" />
            </div>
            <div class="col-md-3">
                <input class="form-control" type="text" id="Vencurrency" placeholder="Currency" />
            </div>
            <div class="col-md-2 d-flex align-items-center gap-3">
                <button class="btn btn-bt-primary" type="button" id="btnSubmit">Add +</button>
                <div class="bt-action-icons">
                    <i id="btnVenEdit" class="ri-save-3-fill text-success" onclick="javascript:SaveEditVen();" style="display:none" title="Edit"></i>
                    <i id="btnVenDel" class="ri-delete-bin-2-fill text-danger" onclick="javascript:DeleteVen();" style="display:none" title="Delete"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="table-responsive text-nowrap">
        <table id="bindDataTable" class="table table-hover" width="100%">
            <caption class="visually-hidden">Vendor configuration</caption>
            <thead>
                <tr>
                    <th>Vendor Name</th>
                    <th>Vendor Code</th>
                    <th>Currency</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                <tr>
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
            </tfoot>
        </table>
    </div>
</div>

<!-- User permission warning modal -->
<div class="modal fade" id="modalAuth" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="authwarning" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title text-danger" id="authwarning">User Permission</h5>
            </div>
            <div class="modal-body">
                You don't have this permission
            </div>
            <div class="modal-footer">
                <button type="button" id="btnModalClose" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

@section PageScripts {
    <script src="~/js/jquery.js"></script>
    <script src="~/js/jquery.min.js"></script>
    <script src="~/js/datatables.js"></script>
    <script src="~/js/datatables.min.js"></script>
    <script src="~/js/vendorlist.js"></script>
}
